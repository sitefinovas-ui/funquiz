{
  "devDependencies": {
    "prettier": "^3.6.2"
  },
  "scripts": {
    "format": "npm run --prefix FUNQUIZ format && npm run --prefix SERVER_FUNQUIZ format",
    "format:check": "npm run --prefix FUNQUIZ format:check && npm run --prefix SERVER_FUNQUIZ format:check"
  },
  "dependencies": {
    "bootstrap": "^5.3.8",
    "lucide-react": "^0.545.0",
    "react-bootstrap": "^2.10.10"
  }
}
